extends layout

block content
	h1
		Standing Orders:
	#orders
		each order, i in orders
			div(id= order._id)
				| Customer name: #{order.customer}
				form(method='post', class='complete', action='/order/complete')
					input(type='hidden', name='id', value= order._id );
					input(type='submit', value='Complete Order')
